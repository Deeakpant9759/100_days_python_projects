def minDistance(word1, word2):
    def helper(index1,index2,num):
        if word1 == word2:
            return Count
        if word1[index1]==word2[index1]:
            return 
        insert = helper()
    return helper(0,0,0)
    